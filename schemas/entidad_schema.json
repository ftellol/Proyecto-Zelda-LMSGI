{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "EntidadZelda",
  "description": "Valida la estructura de una entidad devuelta por la Zelda Fan API",
  "type": "object",
  "required": ["id", "name"],
  "properties": {
    "id": {
      "type": "string",
      "description": "Identificador único de la entidad"
    },
    "name": {
      "type": "string",
      "description": "Nombre de la entidad"
    },
    "description": {
      "type": ["string", "null"],
      "description": "Descripción de la entidad"
    },
    "appearances": {
      "type": "array",
      "items": {
        "type": "string",
        "format": "uri"
      },
      "description": "URLs de los juegos en los que aparece"
    },
    "gender": {
      "type": ["string", "null"],
      "description": "Género del personaje. Solo presente en characters"
    },
    "race": {
      "type": ["string", "null"],
      "description": "Raza del personaje. Solo presente en characters"
    },
    "dungeons": {
      "type": "array",
      "items": {
        "type": "string",
        "format": "uri"
      },
      "description": "URLs de los dungeons relacionados. Solo presente en bosses"
    },
    "inhabitants": {
      "type": "array",
      "items": {
        "type": "string",
        "format": "uri"
      },
      "description": "URLs de los habitantes del lugar. Solo presente en places"
    }
  },
  "additionalProperties": true
}